{
  "name": "fitness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal food and fitness tracker with a brutally honest daily coach",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:worker": "wrangler dev",
    "setup": "node scripts/setup.mjs",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute fitness --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute fitness --local --file=./schema.sql",
    "test": "node --test",
    "db:migrate": "wrangler d1 execute fitness --remote --file=./migrations/002-multi-tenant.sql -y",
    "db:seed": "wrangler d1 execute fitness --remote --file=./migrations/003-plans-and-katy.sql -y"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
